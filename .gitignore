# ==========================================================
# Jekyll / GitHub Pages — generated site output
# ==========================================================
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# ==========================================================
# Ruby / Bundler
# ==========================================================
.bundle/
vendor/

# ==========================================================
# Node / tooling (not used by the Jekyll site, but ignored so
# local experiments — an Astro scaffold, screenshot tooling —
# can never leak into the tracked tree)
# ==========================================================
node_modules/
.astro/
.playwright-mcp/
dist/
src/
public/

# Notion sync — secrets (no .state.json; script derives from committed files)
.env

# ==========================================================
# Logs
# ==========================================================
*.log

# ==========================================================
# OS / editor cruft
# ==========================================================
.DS_Store
Thumbs.db
*.swp
*~

# Editor / agent local state
.claude/